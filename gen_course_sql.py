import pandas as pd

# generate the sql files with course information from the courses.tsv file

# first compile values for the courses table as they are referenced in the sections table

# generate insert statements for sections table